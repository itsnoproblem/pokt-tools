export type PocketError = {
    applicationPublicKey: string,
    blockchain: string
    bytes: number,
    code: number,
    elapsedTime: number,
    message: string,
    method: string,
    timestamp: Date
}